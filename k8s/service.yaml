apiVersion: v1
kind: Service
metadata:
  name: muster-api
  labels:
    app: muster-api
spec:
  selector:
    app: muster-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
